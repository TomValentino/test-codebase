{
  "name": "instant-shopify-storefront",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node engine/build.js",
    "demo": "node engine/build.js --serve",
    "cloudflare:preview": "npm run build && wrangler pages dev",
    "cloudflare:deploy": "npm run build && wrangler pages deploy",
    "test": "npm run build"
  },
  "dependencies": {
    "solid-js": "1.9.14"
  },
  "devDependencies": {
    "vite": "8.2.0",
    "vite-plugin-solid": "2.11.13",
    "wrangler": "^4.0.0"
  }
}
